spring:
  application:
    name: user-microservice
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8580/realms/tutorial-realm"
